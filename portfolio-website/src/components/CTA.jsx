const CTA = () => {
  return (
    <div className="cta">
        <a className="btn" href="">Download CV</a>
        <a className="btn btn-primary" href="#portfolio">My Projects</a>
    </div>
  )
}

export default CTA